# Gateway 重启单飞修复计划

1. BUG-01：在 `GatewayProcess` 增加进程级异步重启闸门，合并并发 Rust 命令。
2. BUG-02：增加可复用的前端单飞协调器，适配器的重启 API 全部经过它。
3. BUG-03/04：拆分诊断日志与重启生命周期事件，让 UI 同步进入和退出重启状态。
4. BUG-05：扩大启动页和离线页的恢复日志区域，保留有效上下文。
5. BUG-06：恢复萌宠蓝色 UI 强调色，不改变皮肤本体颜色。
6. 每类问题补回归测试，再运行 TypeScript、前端测试、Rust 测试和生产构建。
