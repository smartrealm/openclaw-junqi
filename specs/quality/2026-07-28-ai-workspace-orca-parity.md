# AI 工作台 Orca 对照改造规格

状态：代码与自动化完成

日期：2026-07-28

## 目标行为

- AI 工作台保留项目栏、任务栏、中间内容区、右侧活动栏和底部终端的现有结构。
- 右侧 Git 变更面板表示当前 `projectPath` 或任务 worktree 的真实 Git status，不提供无数据依据的任务归属筛选。
- 路径筛选匹配完整仓库相对路径，不区分大小写；空白查询等价于未筛选。
- 面板在可见窗口中立即刷新并按固定间隔刷新；隐藏窗口不发起轮询；恢复可见后立即刷新。
- 同一面板最多有一个 `git_status` 请求在途；在途期间收到的刷新信号合并为一次后续刷新。
- 手动刷新、stage、unstage、discard 和 commit 继续使用同一刷新入口。
- 所有可见文字和 tooltip 使用应用 i18n。
- 全局命令面板必须把 Lucide `forwardRef` 图标作为组件渲染，不能把图标对象作为 React 子节点。

## 验收

- [x] `currentTaskCreatedAt`、`taskChanges` 和“当前任务”伪页签已删除。
- [x] Filter 按钮可打开筛选输入、清空并关闭。
- [x] 工作区无变更与筛选无匹配显示不同空状态。
- [x] 可见刷新生命周期和路径筛选有行为回归测试。
- [x] 三套当前启用的 locale 包含完整 Git 变更、历史和 diff 文案。
- [x] 命令面板图标契约统一，并有 `forwardRef` 图标渲染回归测试。
- [x] 全量 TypeScript、边界、测试、构建和 diff 检查通过。

## 未验证边界

Tauri 走查已复现并验证命令面板崩溃修复，但调试实例可能复用同名窗口状态；为避免继续干扰现有会话，已停止桌面自动化并恢复临时 WebKit 状态。真实 Git 自动刷新、筛选和 stage/discard 交互没有做真机验收，不得据此声称已通过。
