@AGENTS.md

# Claude Code Project Instructions

上面的 `AGENTS.md` 是本仓库所有编码代理共享的规范，必须完整遵守。本文件只补充 Claude Code 专属约定，避免两套工程规则产生漂移。

## 文件边界

- 这里只写 Claude Code 的会话、模式和工具行为。
- 项目规则（Emoji 禁止、文档先行、工程边界、常用命令、测试与验证、Git 与发布、完成标准）只在 `AGENTS.md` 维护，不要在本文件重复、摘录或改写。
- 发现长期有效的项目纠正时，写回 `AGENTS.md` 或对应领域文档，不要只留在 auto memory 或单次会话里。
- 跨模块改动前用 `/context` 确认 `CLAUDE.md` 与 `AGENTS.md` 均已加载。

## 长期工程决策原则

- 不保留向后兼容。过时实现直接删除，不增加兼容层、迁移逻辑或 fallback。
- 选择满足当前已确认需求的最简单实现。禁止预防性抽象、无消费者的配置层和为假设性场景增加复杂度。
- 系统按层次递进：先交付可验证的最小端到端链路，再基于真实需求扩展；不得为了未完成的复杂度拆除已可运行的链路。
- 组件和模块必须保持单一关注点与清晰边界；复用已有边界，不以跨层耦合换取短期实现速度。
- 优先使用成熟、持续维护且与当前依赖兼容的库。自行实现前必须有明确的契约、维护或安全理由。
- 新增依赖或自研前，先审查仓库现有依赖、共享组件、工具和官方能力是否已满足需求；不得无证据假定现有能力缺失。
- 架构决策以长期可维护性、可验证性和真实边界为准。禁止以“先这样以后再换”为理由引入临时路径、临时协议或临时代码。
- 设计前先研究成熟产品、官方文档和已验证模式如何解决同类问题；在不违背项目与 OpenClaw 契约的前提下优先复用，而非从零发明。

## 模式与意图

- 对跨前端/Rust、安装、Gateway 生命周期、OpenClaw Wizard、凭据、发布或多文件状态机的改动，先进入 plan mode。
- 退出 plan mode 前必须已完成只读证据收集，并写出文件范围、改动顺序和可执行的验证命令。不得把“先改再看”当作计划。
- 用户要求审查、解释或定位时保持只读，不要顺手修改。只有明确要求修改、构建或发布时才执行对应写操作。
- 多步骤任务持续核对最新用户要求。用户中途调整范围时以最新指令为准，不继续执行已作废的计划。

## 工具使用

- 优先使用 Read/Grep/Glob 等专用工具，不要用 `cat`、`sed`、`awk`、`echo` 读写文件。
- 无依赖关系的调用在同一轮并行发起；有依赖的必须等前一步的真实返回结果。
- 未经用户明确要求不派生 subagent。即便使用 subagent，主会话仍要自己读规范文件、确认关键契约并审查最终 diff。
- subagent 结论和工具摘要都不是事实来源。与仓库源码或实际运行输出冲突时，以源码和运行证据为准。

## 证据与报告

- 引用外部行为时优先给出官方文档或官方源码链接，并在相关 audit/spec/plan/validation Markdown 中保存版本或 commit 依据。
- 完成前重新读取 `git status` 和实际验证输出。不要根据命令已启动、文件已生成或子任务已返回就推断成功。
- 报告时区分已执行和未执行的验证，未运行的命令不得写成已通过。
- 共享工作区可能存在并行改动。给出结论前确认 diff 中没有混入他人或其他任务的内容。

## 本机产物

- `.claude/`、`.learnings/` 等本机代理状态目录不是项目规范或业务源码，不要编辑或提交。
- 正式记录仍走 `docs/`、`specs/`、`plans/`。`.planning/` 下已跟踪的是既有专项记录，不要把插件生成的临时产物新增进仓库。

<!--
Claude Code 官方依据：
https://code.claude.com/docs/en/memory
该文档明确建议项目级 CLAUDE.md 保持简洁，并在已有 AGENTS.md 时通过 @AGENTS.md 导入共享规则。
-->
