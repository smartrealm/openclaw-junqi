# Gateway 状态权威边界整改计划

日期：2026-08-09

## 实施顺序

1. 更新状态机测试，使其只提交 `processAlive` 与 `endpointReady`。
2. 收紧 `GatewayEvent` 判别联合并删除状态机回退。
3. 将子智能体活动回归改为证明时间戳和状态文案不能生成运行事实。
4. 删除不再有消费者的时间解析、状态归一化和宽限期常量。
5. 运行状态机、Gateway 数据 store 定向测试，再进入完整验证。
