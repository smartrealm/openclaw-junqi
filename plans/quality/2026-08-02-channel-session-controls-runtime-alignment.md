# 渠道与会话控制运行时对齐计划

1. 从当前 OpenClaw 协议源码核对方法权限和渠道 UI 元数据。
2. 把渠道管理员操作收敛到现有临时高权限连接，并添加回归测试。
3. 使用 `channels.status` 元数据渲染渠道图标和已配置状态。
4. 按协议区分原生会话写操作与 JunQi 本机视图偏好，补齐本机归档恢复入口。
5. 运行 TypeScript、边界检查、定向测试和构建验证；真实管理员授权与真实渠道账号另行验收。
