# OpenClaw 会话生命周期收敛计划

日期：2026-08-06

## 实施顺序

1. [x] 建立严格、分页的官方会话列表客户端和单一行投影。
2. [x] 修复 Gateway 状态仓完整比较与活动会话已读幂等。
3. [x] 保存 `agents.list.defaultId` 并删除 `main` 默认 Agent 假设。
4. [x] 为会话 patch 加入 `expectedSessionId` 身份围栏。
5. [x] 收敛普通创建、完整分叉、消息分叉和检查点分支提交入口。
6. [x] 删除本地 topic 持久化、旧存储迁移和旧 Gateway 回退。
7. [x] 补充每个问题对应的行为回归测试。
8. [ ] 执行静态检查、完整测试、生产构建和差异检查（静态检查、生产构建和差异检查已通过，完整测试受既有 Provider 失败阻断）。
