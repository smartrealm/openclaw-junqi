# OpenClaw 会话目标只读投影计划

1. 核对官方目标类型、Gateway 会话行计算与稳定协议 schema 边界。
2. 新增严格解析和与本地 Task 图隔离的只读会话投影。
3. 在标签页与当前会话栏显示状态，并补充完整快照清除回归。
4. 执行定向与全量验证后提交。

## 非目标

- 不增加目标创建、暂停、恢复、完成或预算写入入口。
- 不把 JunQi 本地检查点或协作工作流写回 OpenClaw 会话。
