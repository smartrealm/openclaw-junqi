# OpenClaw 会话文件只读投影对齐计划

1. [x] 审计官方 schema、Gateway handler、Control UI 和 JunQi 本地工作区边界。
2. [x] 实现连接与会话围栏的 `sessions.files.list`、`sessions.files.get` client。
3. [x] 接入只读会话文件面板与安全预览。
4. [x] 补齐回归、多语言、验证记录和中文提交。
