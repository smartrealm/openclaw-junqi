# OpenClaw Hook 状态只读投影计划

1. 审查官方 Hook 请求 schema、方法权限、handler 和报告结构，确认原始路径与需求详情不能进入 JunQi UI。
2. 建立连接身份围栏的只读客户端，仅解析安全状态字段。
3. 在维护中心加入按需读取面板，不加入任何 Hook 管理写操作。
4. 补充客户端、界面和边界回归测试，完成全量验证后记录结果并中文提交。

## 非目标

- 不通过 JunQi 安装、启用、禁用、重载、运行或删除 OpenClaw Hook。
- 不读取或保存 Hook 文件、工作区目录、处理器路径、环境变量值或配置内容。
