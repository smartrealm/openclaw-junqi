# 本地 main 对齐与客户端边界实施计划

## 顺序

1. [x] 读取根规范、README、审计记录、共同祖先和 `main` 双向提交。
2. [x] 合并 `main` 的主题收敛，保留语义化 token，拒绝全局颜色重绑并修复遮罩派生。
3. [x] 重新检测 `main` 前进到 `23864b18565ed37ef70bd8871aeacc75bb993134`，继续合并文件预览与版本改动。
4. [x] 建立工作区文件入口、页签状态、Agent Hub 和共享预览的生产引用图。
5. [x] 删除已被当前工作区替换的旧页面、无消费者 reducer、专属测试和失真文档。
6. [x] 将 JSON 预览收敛到共享能力解析和保真格式化，将 Agent Hub 源码匹配测试改为组件行为测试。
7. [x] 运行完整 TypeScript、构建和版本一致性验证，完成第二次合并提交。
8. [ ] 恢复语音能力边界暂存改动，继续按 OpenClaw 官方源码审查并完成最终全量验证。
9. [ ] 扫描无引用代码、英文新增注释、Emoji 和差异格式，使用中文提交最终结果。

## 保护条件

- 不使用分支覆盖来解决冲突；每个保留或删除项必须有调用链与契约依据。
- 不把本机 CLI、当前平台或本机 Gateway 当成跨平台能力来源。
- 不恢复已删除的 `McpTools` 页面或其专属本地化。
- 不用固定路径、平台判断、经验阈值或展示文案伪造运行时能力。
