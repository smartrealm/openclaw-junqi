# 本地 main 对齐与客户端边界实施计划

## 顺序

1. [x] 读取根规范、共同祖先、双向提交、文档和冲突预演。
2. [x] 合并本地 `main`，以当前分支的会话和请求生命周期契约解决冲突。
3. [x] 建立 `ego-lite` Provider 的全局引用图，确认其仅由本次合并引入。
4. [x] 删除不属于 OpenClaw 原生能力的 Provider、Tauri 探测、页面挂载、测试、本地化和文档。
5. [x] 审计保留的 IPC wrapper、Gateway 类型收敛、页面调用方和 Rust command 注册。
6. [x] 运行全量验证、扫描无引用项和 Emoji，提交中文合并提交。

## 保护条件

- 不使用分支覆盖来解决冲突；每个保留或删除项必须有调用链与契约依据。
- 不把本机 CLI、当前平台或本机 Gateway 当成跨平台能力来源。
- 不恢复已删除的 `McpTools` 页面或其专属本地化。
