# OpenClaw 会话标签权威性计划

日期：2026-08-06

1. [x] 核对 OpenClaw `sessions.create` 响应和 `sessions.list` 行的 label 归属。
2. [x] 审计 JunQi 新建入口、会话投影、tab 与侧边栏展示。
3. [x] 删除语言相关占位符猜测，统一保留 Gateway label。
4. [x] 添加非空 Gateway label 保持、普通创建省略 label 与缺失 label 回退测试。
5. [x] 收敛侧栏、标签页、仪表盘、活动和时间线到共享展示函数，删除重复标题逻辑。
6. [x] 保留活动聚合的空标签语义，删除将 key 写入 `label` 的投影。
7. [x] 清除页面级固定语言兜底，改为翻译函数注入。
8. [x] 执行合并后的定向测试、lint 和差异检查；生产构建与桌面真机验收仍待执行。
6. [ ] 执行生产构建与桌面真机验收。
