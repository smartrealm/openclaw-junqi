# 发布 CI 与安装包构建收敛计划

1. 从 `.github/workflows/ci.yml` 删除 Windows x86/x64 原生矩阵，并收敛 CI summary 依赖。
2. 从标签工作流的前置验证中移除等待 CI 的轮询，使五目标构建可立即开始。
3. 在标签工作流每个 Windows 目标的安装包构建前执行锁定依赖下的 Rust 库测试。
4. 将同提交 CI 成功检查移到 `publish`，使其仍是公开 Release 的硬门禁。
5. 运行工作流语法检查、相关脚本测试和差异检查；下一次标签发布验证实际并行行为。
