# 工作区首屏数据就绪实施计划

## 实施项

- [x] 审核 App 启动门禁、会话快照和共享轮询数据组的关系。
- [x] 将就绪条件收紧为当前 Gateway 连接后的会话和智能体成功快照。
- [x] 将首批读取失败改为全局失败与重试，移除空工作区放行路径。
- [x] 复用共享 `AppLoadingFallback` 呈现加载和失败状态。
- [x] 增加三语言文案和启动门禁回归测试。
- [ ] 在 macOS、Windows、Linux 的冷启动和 Gateway 断线恢复场景完成真机验证。

## 验证顺序

1. 运行 App 启动回归测试和 TypeScript 类型检查。
2. 运行模块边界检查、完整前端测试、生产构建和 `git diff --check`。
3. 记录桌面真机的首屏加载、失败重试和可选路由行为。
