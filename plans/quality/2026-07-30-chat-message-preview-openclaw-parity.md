# Chat 消息预览与 OpenClaw 对齐实施计划

1. 提取共享消息预览数据类型、状态 hook 与预览面板。
2. 将消息动作图标从产物眼睛图标改为消息级 `PanelRightOpen`。
3. 在主 ChatView 增加可关闭、响应窗口宽度的右侧分栏。
4. 在 Quick Chat 复用同一面板并使用窄窗口覆盖布局。
5. 删除零引用的重复产物结果卡。
6. 增加消息动作、预览状态和布局回归测试。
7. 运行相关测试、lint、完整测试、build 和 `git diff --check`。
