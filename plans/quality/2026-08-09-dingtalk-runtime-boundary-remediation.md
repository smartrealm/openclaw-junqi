# 钉钉扩展运行时边界整改计划

日期：2026-08-09

## 实施顺序

1. 为失败后重新解析增加真实临时文件回归测试。
2. 将解析状态拆为成功缓存与飞行中 Promise，失败时只清理飞行中状态。
3. 删除运行状态投影中的绝对路径字段并补充回归。
4. 将错误序列化收敛为错误码目录、固定文案和详情白名单。
5. 删除子进程启动原因和 DWS 原始错误文本提取路径。
6. 运行钉钉单元测试、包契约校验、TypeScript 检查和完整构建。
