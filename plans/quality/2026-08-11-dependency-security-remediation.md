# 依赖安全收敛计划

1. 读取远端告警和本地审计结果，建立直接依赖与传递依赖路径。
2. 核对 npm 包元数据中的修复版本、Node 引擎和 React 兼容范围。
3. 升级直接依赖；为 OpenClaw 精确传递依赖配置 pnpm 9 版本选择器覆盖。
4. 使用项目锁定的 pnpm 9 重新生成锁文件，执行冻结安装、审计、类型检查、测试和构建。
5. 提交后重新查询远端 Dependabot 告警，区分本地验证和远端扫描状态。
