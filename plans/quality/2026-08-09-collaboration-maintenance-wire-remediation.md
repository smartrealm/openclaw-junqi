# 协作维护线协议收敛计划

日期：2026-08-09

## 实施顺序

1. 删除插件维护投影与退出响应中的 `active`。
2. 桌面维护协调器只校验 `gateActive`，并在边界映射内部 `active`。
3. capabilities 解码器改为读取 `gateActive`。
4. 更新测试夹具并增加旧别名拒绝测试。
5. 运行维护协调器、客户端、线协议和协作插件测试。
