# OpenClaw 模型目录权威性计划

1. [x] 核对当前安装 OpenClaw 的 `models.list` 文档、运行时结果构建和 Control UI 调用。
2. [x] 审查 JunQi 模型加载、状态存储和模型选择调用方，确认旧回退可成为可选操作。
3. [x] 移除会话模型选择器的配置、静态目录和 session fallback，仅保留严格 Gateway 结果解码。
4. [x] 增加权威空、无效、不可用和可用模型的回归测试。
5. [x] 执行分层验证、清理扫描并提交中文提交。
