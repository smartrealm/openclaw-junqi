# JunQi Desktop Application Autostart Plan

1. [x] Add and initialize the installed Tauri autostart plugin with the
   platform-native desktop launcher: LaunchAgent on macOS, Run entry on
   Windows, and XDG autostart entry on Linux.
2. [x] Expose typed Rust commands for application autostart status, enable, and
   disable without changing Gateway service commands.
3. [x] Add the Ready-screen application login-autostart row, localized copy,
   operation state, and navigation gate.
4. [x] Add focused regressions, run TypeScript and Rust validation, and record
   the platform-specific login-item boundaries.
