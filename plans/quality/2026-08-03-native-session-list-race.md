# 原生新建会话列表竞态修复计划

1. [x] 核对当前安装 OpenClaw 的 `sessions.create`、`sessions.list` 和官方 TUI 新建后选中行为。
2. [x] 沿 JunQi 的 create、store、列表轮询和标签回退路径确认旧完整快照可移除刚创建的活动会话。
3. [x] 在会话生命周期工具中增加确认创建通知，并让正常新建和 checkpoint 分支在本地提交后发送通知。
4. [x] 在 `App.tsx` 收到通知时废弃旧列表请求并立刻开始新的权威会话列表读取。
5. [x] 在 `chatStore` 增加会话投影版本；让完整快照只在请求开始后版本未变化时删除缺失会话或回退 session identity。
6. [x] 覆盖确认后通知、request gate、旧快照保留新会话和新快照真实删除会话。
7. [x] 运行边界检查、完整测试、生产构建和差异检查，并记录结果。
