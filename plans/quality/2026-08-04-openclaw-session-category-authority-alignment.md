# OpenClaw 会话 category 权威性计划

1. [x] 核对 OpenClaw 当前 method registry、schema、sessions handler 和 patch projection。
2. [x] 审查 JunQi Gateway client、chat store、侧栏、菜单、Jarvis 唤醒和文档调用图。
3. [x] 删除不存在的 `sessions.groups.*` client、状态、UI 和文案，保留 category patch。
4. [x] 让 category 投影验证 Gateway 返回 entry，并由 session snapshot 派生分类列表。
5. [x] 补回归、执行全量验证、扫描无引用项并中文提交。
