# Provider Catalog 构建可复现性实施计划

日期：2026-08-03

1. [x] 核对生成脚本、工作区 OpenClaw 依赖、锁定版本和构建入口，确认普通构建会改写受控生成物。
2. [x] 将普通构建改为仅在生成物缺失时生成，保留显式更新 Catalog 的命令。
3. [x] 为默认再生成增加工作区精确版本校验，并移除从 `PATH` 自动选择 CLI 的行为。
4. [x] 更新回归测试和现有发布构建说明。
5. [x] 运行脚本测试、完整构建和差异检查，确认普通构建不改写生成物。
