# Gateway 连接异常 UI 改造计划

日期：2026-07-28

## 任务

- [x] 核对连接关闭、重试、状态 store 和全局错误覆盖层的数据流。
- [x] 为预期的传输生命周期终止建立窄类型错误。
- [x] 提取可测试的全局 Promise 拒绝策略。
- [x] 让普通断连继续由共享连接状态 UI负责。
- [x] 补齐后台队列和会话重同步的 Promise 终点。
- [x] 增加 recoverable/fatal 分流回归测试。
- [x] 执行完整 TypeScript、测试、生产构建和本机 macOS 打包。
- [ ] 执行 Tauri 真机断连/重连走查。

## 回滚边界

不修改持久化数据和 OpenClaw 协议。回滚只涉及前端错误分类、连接层拒绝值和后台 Promise 收口。
