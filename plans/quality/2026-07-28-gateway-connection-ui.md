# Gateway 连接异常 UI 改造计划

日期：2026-07-28

## 任务

- [x] 核对连接关闭、重试、状态 store 和全局错误覆盖层的数据流。
- [x] 为预期的传输生命周期终止建立窄类型错误。
- [x] 提取可测试的全局 Promise 拒绝策略。
- [x] 让普通断连继续由共享连接状态 UI负责。
- [x] 补齐后台队列和会话重同步的 Promise 终点。
- [x] 增加 recoverable/fatal 分流回归测试。
- [x] 区分 Gateway 进程探测、运行时就绪和认证连接完成进度。
- [x] 用类型化进度对象收口插值参数、运行态和终态。
- [x] 让自动和手动恢复在连接管理器成功/耗尽时统一收敛。
- [x] 增加状态栏过期运行态门禁和 BUG-GW-UI-02 回归。
- [x] 执行完整 TypeScript、测试、生产构建和本机 macOS 打包。
- [ ] 执行 Tauri 真机断连/重连走查。

## 验证结果

- 前端 1829 项与脚本 223 项通过；`pnpm lint`、`pnpm build` 和 `git diff --check` 通过。
- 构建最大 JavaScript chunk 为 513.31 kB，无循环 chunk 或超限警告。
- `1.4.17` ARM64 本地 DMG 已通过镜像、版本、架构和 ad-hoc 签名核验。
- 自动化浏览器环境没有可用窗口，实际断连/重连状态转换保留为待验证。

## 回滚边界

不修改持久化数据和 OpenClaw 协议。回滚只涉及前端错误分类、连接层拒绝值和后台 Promise 收口。
