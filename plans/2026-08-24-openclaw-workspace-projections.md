# OpenClaw 工作台投影实施计划

1. 为 Cron 页面补充只读例行工作总览，复用现有 Cron Store、状态和运行记录解析。
2. 在活动中心收敛任务、审计、会话运行和会话级有效工具的只读关联视图。
3. 以 Task Ledger 增加只读状态流投影，保留现有任务详情和官方恢复操作。
4. 审查 Git Tauri command、调用方和路径来源后，实现受控工作区和写操作边界。
5. 对每层执行定向测试、TypeScript 静态检查、构建和差异检查，并在状态文档记录未完成真机验证。
