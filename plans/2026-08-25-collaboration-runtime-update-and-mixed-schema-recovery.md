# 协作运行时更新与混合 schema 恢复计划

## 执行顺序

1. [x] 增加旧插件启动失败但版本落后时的设置决策回归，证明旧顺序封死更新。
2. [x] 调整无活动恢复事务下的决策顺序，同版本服务失败继续失败关闭。
3. [x] 增加精确混合 schema 13 成功迁移和错误索引拒绝回归。
4. [x] 为 schema 13 增加仅限权威索引定义的混合结构门禁，复用现有备份和事务迁移。
5. [x] 收敛办公室与设置页错误文案，不在无恢复事务时预判回滚。
6. [x] 重建 `0.5.5` 协作固定归档和 macOS Apple Silicon 本地测试 DMG，运行插件、前端、构建、镜像与差异验证；本次未修改 Rust 源码。
7. [ ] 通过新桌面安装包应用固定包、重启实际 Gateway，核对运行时插件版本、数据库 metadata、备份和智能体办公室。
8. [x] 将插件回滚窗口限制到健康确认前；确认成功和回滚重启成功后删除旧插件归档、配置备份与暂存包，并关闭恢复状态。
9. [x] 增加正常清理和符号链接边界失败回归，确认日志与磁盘制品同步收敛。

## 严格代码质量审查

- [x] 使用 `thermo-nuclear-code-quality-review` 审查完整变更。
- [x] 将新增后超过 1000 行的数据库测试文件收敛到 967 行，历史 schema 构造器提取为测试专用夹具。
- [x] 确认生产兼容逻辑位于 schema 初始化边界，没有新增跨层 fallback、宽松类型或非事务写入。

## 停止条件

- 目标、连接、桌面所有权或持久化运行身份不能核验时，不部署插件。
- 混合 schema 13 的索引定义与当前权威不完全一致时，不迁移数据库。
- 备份、事务围栏、Gateway 重启或运行时能力核验任一失败时，不声明恢复完成。
- 健康确认后的事务制品不能安全删除时，报告清理失败且保持恢复入口关闭；不得重新暴露没有完整制品的回滚动作。
