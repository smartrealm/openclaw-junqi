# 协作 schema 12/14 恢复计划

## 执行顺序

1. 根据仓库历史 schema 12 定义补充精确对象门禁，拒绝结构漂移。
2. 将 schema 12 接入现有备份、事务围栏、收据归并和墓碑重建路径。
3. 增加 schema 12 成功迁移与失败关闭的数据库回归测试。
4. 将钉钉运行页的内部核验证据收敛为默认收起的技术详情，并增加渲染回归。
5. 重新生成协作固定归档和 metadata，运行插件、前端、构建及差异验证。
6. 按提交 `27411b08` 修正 schema 14 的 `commands_available` 对象门禁与测试夹具，先证明旧实现拒绝真实结构，再验证迁移和备份。

## 停止条件

仅历史提交可证明的 schema 12、13、14 允许迁移。任何不匹配的对象集合、未知版本、收据冲突、
无效墓碑或备份失败都停止并保持原库不变。
