# 首次启动凭据授权计划

日期：2026-08-07

1. 审查共享 token、设备 token 与设备签名在 Gateway 握手中的实际使用顺序。
2. 让共享 token 路径跳过设备 token 的系统凭据读取和首屏自动写入。
3. 将 JunQi 自有设备身份从系统凭据库迁移为应用私有状态文件，并保留跨平台私有权限。
4. 让工作区首屏只依赖会话快照，智能体列表继续后台同步。
5. 补充解析器、握手和工作区首屏回归测试。
6. 执行定向测试、TypeScript 检查、Rust 检查、边界检查和生产构建。
