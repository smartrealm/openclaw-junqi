# 目标环境证据与副作用链路修复计划

## 执行顺序

1. BUG-01：拆分公共 npm 元数据解析与基于已选 Node/npm 的配置解析，删除 OpenClaw 首次安装前的 `LegacyFallback` bootstrap 写入。
2. BUG-01：补充 Node.js 目标元数据来源选择的单元回归。
3. BUG-02：在 DWS runner 启动前检查中止信号，将信号交给 `spawn`，并为已启动写操作增加待核验错误语义。
4. BUG-02：补充预先中止和公开错误序列化的行为回归。
5. BUG-03：在运行时切换补偿前核验先前 Gateway 是否真实运行，未运行时禁止调用恢复入口。
6. BUG-03：补充失败安装不恢复未运行 Gateway 的事务行为回归。
7. BUG-04：删除 npm 默认全局前缀安装，只接受用户明确选择的 OpenClaw npm 目录。
8. BUG-04：将 npm 缓存目录绑定到显式 npm 安装方式，并删除无消费者的全局前缀投影和文案。
9. 对修改文件进行类型、格式、Emoji 和全局旧路径核对；运行验证仅能由目标系统的独立证据完成。
