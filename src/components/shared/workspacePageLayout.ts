/** 数据与管理页面使用路由视口的完整可用宽度，局部阅读宽度由内容组件自行约束。 */
export const WORKSPACE_PAGE_FRAME_CLASS_NAME = 'min-h-full w-full min-w-0 p-3 sm:p-5';

/** 带独立顶栏的页面复用相同的水平边距，不为页面单独设置画布上限。 */
export const WORKSPACE_PAGE_CONTENT_CLASS_NAME = 'w-full min-w-0 px-3 sm:px-5';
